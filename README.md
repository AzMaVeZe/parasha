# בין הנכתב לנגלה — על פרשת השבוע · אריאל ז'יטניצקי

אתר סטטי חדש לארכיון דפי פרשת השבוע, המחליף את הבלוג הישן ב-Blogspot
(https://ariel-parasha.blogspot.com/). העיצוב ממומש מתוך פרויקט
"פרשת השבוע אריאל — Design System" ב-Claude Design: קלף, כחול־דיו וזהב,
טיפוגרפיה עברית (Frank Ruhl Libre / Heebo / David Libre), RTL מלא.

## מה יש באתר

- **ארכיון מלא לפי ספר** — חמשת חומשי תורה + חגים ומועדים, עם חיפוש פרשה
  (כולל נרמול גרשיים: "טו באב" ימצא את ט"ו באב).
- **פרשת השבוע** — באדג' בהירו עם הפרשה/החג הקרוב (Hebcal, לוח ישראל),
  כפתור ישיר לדף השבוע ותצוגה מקדימה חיה שלו.
- **צפייה והורדה** — לפי `js/pdf-manifest.js` (נוצר ב-`scripts/build-pdf-manifest.sh`;
  להריץ אחרי כל הוספת קובץ). במסכי מגע הדף נפתח בלשונית מלאה (מגבלת iOS).
- **פודקאסט לכל דף** — כפתור "האזנה" מופיע אוטומטית כשיש מקור: קישור לפרק
  ספוטיפיי (`spotify:` ב-`js/data.js`, מוטמע כנגן + קישור לפתיחה באפליקציה)
  או קובץ שמע ב-`assets/audio/` (ראו ה-README שם ליצירה ב-NotebookLM והעלאה).
- **חידה לשולחן שבת מתחלפת** לפי הפרשה, עם פתרון חידת שבוע שעבר (`js/riddles.js`).
- **הרשמה לרשימת תפוצה** ו**תגובות לכל דף** — שניהם על אותו Cloudflare Worker, ראו למטה.
- **נגישות**: דלג-לתוכן, ניהול פוקוס במודאל, ניגודיות AA, `prefers-reduced-motion`,
  הודעות סטטוס לחיפוש, והצהרת נגישות ב-`accessibility.html`.

## רשימת תפוצה (Resend + Cloudflare Worker)

טופס ההרשמה באתר (מייל + בחירת יום: שני או חמישי) שולח ל-Worker שמאחסן את
הנרשמים ושולח את דף הפרשה בימים שנבחרו. הוראות ההתקנה המלאות:
[`worker/README.md`](worker/README.md). לאחר הפריסה יש להזין את כתובת
ה-Worker ב-`SUBSCRIBE_API` שב-`js/data.js` — עד אז הטופס מוסתר.

## תגובות

כל דף פרשה כולל טופס תגובה **בתוך האתר** — הקורא לא עוזב את העמוד. התגובה
נשלחת לאותו Worker ונכנסת כ**ממתינה לאישור**; אריאל מקבל מייל עם כפתורי
"אישור ופרסום" / "דחייה", ורק אחרי אישור התגובה מופיעה באתר. אפשר גם לנהל
הכול מ-`<worker>/admin?key=<ADMIN_KEY>` — רשימה בעברית של הממתינות ושל
המפורסמות, עם כפתור הסרה.

מה מוצג לקוראים: **שם, תאריך וטקסט בלבד.** כתובת המייל של המגיב נשמרת
בצד השרת ולעולם לא נשלחת לדפדפן — הנתיב `/comments` לא מחזיר אותה כלל.

הגנות: honeypot נסתר, הגבלת קצב (3 תגובות ל-IP בחמש דקות), תקרת אורך של
2000 תווים, ואימות ששם הפרשה קיים ב-`assets/parashot.json`.

אין מה להגדיר ב-`js/data.js` — התגובות משתמשות באותו `SUBSCRIBE_API`.
צריך רק להגדיר פעם אחת לאן יישלחו התראות המודרציה:
```
cd worker && npx wrangler secret put OWNER_EMAIL && npx wrangler deploy
```

## מבנה

```
index.html          העמוד הראשי
styles.css          כניסת עיצוב יחידה (מייבאת את tokens/)
tokens/             טוקנים של מערכת העיצוב: צבעים, טיפוגרפיה, ריווח, אפקטים, פונטים
assets/icons/       אייקוני SVG (Lucide, קו 2px)
assets/pdfs/        ארכיון הקבצים (ראו README בתיקייה — הוספת קובץ = כפתורים באתר)
js/data.js          נתוני הפרשות: שם, קישור Box, PDF מקומי, פרק ספוטיפיי, טפסים
js/site.js          רינדור, חיפוש, ניתוב לעמוד דף (#p=), תגובות, פודקאסט (ואניל)
scripts/build-pages.js  מייצר עמוד סטטי לכל דף (/p/<שם>/), sitemap.xml ו-llms.txt

לחיצה על כרטיס פרשה פותחת עמוד ייעודי (כתובת ‎#p=<שם>‎ ניתנת לשיתוף) ובו
הדף (צפייה/הורדה/הדפסה), נגן הפודקאסט אם קיים, והתגובות על אותו דף.

לצד זה יש לכל דף גם **עמוד HTML אמיתי** ב-‎/p/<שם>/‎, שנוצר בבנייה מ-js/data.js:
זו הכתובת שמנועי חיפוש ומערכות AI מאנדקסות, שהרי ‎#p=‎ אינו עמוד נפרד עבורן.
הכתובות הישנות ממשיכות לעבוד, והלחיצה באתר עדיין נשארת בתוך האפליקציה.
```

## הרצה מקומית

```bash
python3 -m http.server 8000
# ואז http://localhost:8000
```

(פתיחת `index.html` ישירות מהדיסק תעבוד חלקית — בדיקת זמינות ה-PDF דורשת שרת.)

## פריסה

האתר סטטי לחלוטין — מתאים ל-GitHub Pages: Settings → Pages → Deploy from branch.
